#!/usr/bin/env python3
"""
BorderFlow - Seed Script
This script is intentionally empty — use your own data via the UI.

To clear all existing data and start fresh, run:
    python scripts/reset.py
"""
print("No seed data loaded. Use the UI to register your own data.")
print("Dashboard → http://localhost:8000/dashboard")
print("Depot API → http://localhost:8001/docs")
